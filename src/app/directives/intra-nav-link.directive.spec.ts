import { IntraNavLinkDirective } from './intra-nav-link.directive';

describe('IntraNavLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new IntraNavLinkDirective();
    expect(directive).toBeTruthy();
  });
});
